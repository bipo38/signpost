# Develop flow

1. Open `docs/plan-mode.md` and apply its rules. **Caller = this file** (`docs/plan.md`).
2. Implement the request.
3. Test it: unit tests via `docs/testing/unit.md`, browser check via `docs/testing/ui.md`.

Not for exploration; that is `docs/prototype.md`.
