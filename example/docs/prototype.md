# Prototype flow

Throwaway UI variants. Do not use `docs/plan.md` until a variant is chosen.

If the brief is ambiguous, open `docs/plan-mode.md`. **Caller = this file** (`docs/prototype.md`). Voice and tokens come from `PRODUCT.md`.
